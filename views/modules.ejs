<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% modules.forEach(module => { %>
            <li><a href="/modules/<%= module.id %>"><%= module.name %></a></li>
        <% }); %>
    </ul>
    <a href="/dashboard">Retour au tableau de bord</a>
</body>
</html>
