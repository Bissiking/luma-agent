<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1>Bienvenue, <%= user ? user.username : 'Visiteur' %>!</h1>
    <% if (user) { %>
        <a href="/dashboard">Accéder au tableau de bord</a> | <a href="/auth/logout">Déconnexion</a>
    <% } else { %>
        <a href="/auth/login">Connexion</a>
    <% } %>
</body>
</html>
